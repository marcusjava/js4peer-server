{
  "name": "peer-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "dev": "PORT=9000 npm start"
  },
  "dependencies": {
    "peer": "0.6.1"
  }
}
